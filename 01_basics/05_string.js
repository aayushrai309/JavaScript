const name = "Aayush Rai";
const repoCount = 50;

console.log(name + " is " + repoCount);
console.log(`my name is ${name} and my repo count is ${repoCount}`);

// these are string methods/builders
console.log(name.length); //to find length of string
console.log(name.toUpperCase()); // to convert string to Upper Case
console.log(name.toLowerCase()); // to convert string to Lower Case
console.log(name.charAt(3)); // to find the which letter is at that position 
console.log(name.indexOf("s")); // to find the index of the passed value